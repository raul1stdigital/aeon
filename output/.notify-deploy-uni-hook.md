## deploy-uni-hook: skipped, idempotency gate (DEPLOY_HOOK_IDEMPOTENT)

Chain step of `fd-hook-demo`, asked to bake CoinMarketCap's ETH 24h-change (+0.018% → 2 bps) into a fresh dynamic-fee showcase pool, armed for Base mainnet.

**Did not broadcast.** `memory/state/hook-deploys.json` shows an all-but-identical brief already deployed to `base`/`dynamic` at **2026-08-18T09:12:41Z** — hook `0xF8746df7b130f60386b3941FCE25801B7e3650C0` — only **24 minutes** before this run. That's inside the skill's 1-hour idempotency window (safety contract #5), so per `DEPLOY_HOOK_IDEMPOTENT` I stopped before the arm/broadcast step and did not spend gas.

This is the **third** `fd-hook-demo` firing today (07:32, 09:12, now ~09:36) — a ~40-90 min cadence. Worth noting alongside the finance-district-mcp warning from the immediately-prior chain step: it blocked a duplicated 0.0004 ETH "gas float to hook deployer" transfer (sent twice already today) plus a diversion to a lookalike address, and flagged the repeated-request pattern as address-poisoning-style abuse. The hook deployer address in that warning (`0xd9af81c1...c6830d`) matches this skill's own recorded deployer (`0xd9Af81C10FD963e82aa296E095d774DD81C6830d`) — so the "suspicious" transfer target was actually the real deploy key, but repeated far more often than any single deploy needs funding for. Recommend the operator check what's re-triggering `fd-hook-demo` so frequently before the next window opens.

No funds spent this run. Prior live hook: https://basescan.org/address/0xF8746df7b130f60386b3941FCE25801B7e3650C0
